{#def tags=["can't find data"] #}

<article class="my-4 divide-x-2">
  {% for tag in tags %}
  <span
    class="inline-block bg-gradient-to-br from-green-300 to-emerald-500 bg-clip-text text-transparent text-sm font-bold px-2"
  >
    {{ tag }}</span
  >
  {% endfor %}
</article>
